<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head')%>
    <title>Listado de Usuarios</title>
</head>
<body class="form">
    <%- include('../partials/headerAdmin')%>
    <div class="usersList"> 
            
    <h1>Listado de Usuarios</h1>
    <ol>
        <% for (let i = 0; i < usuarios.length; i++) { %>
            <li>
                Ingreso: <%= usuarios[i].created_at.toDateString() %>
                 - Nombre: <%= usuarios[i].nombre %>
                 - E-mail: <%= usuarios[i].email %>
                 - Perfil: <%= usuarios[i].perfil.nombre %>
                 - <button class="eliminar-usuario-button" type="button"><a href='/users/destroy/<%= usuarios[i].id %>'>Eliminar</a></button>
            </li>
        <% } %>
    </ol>
    </div>
</body>
</html>